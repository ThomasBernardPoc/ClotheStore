<?xml version="1.0" encoding="utf-8" ?>
<DataTemplate
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Models="clr-namespace:ClotheStore.Models"
    x:DataType="Models:ClotheWrapper"
    x:Class="ClotheStore.Views.DataTemplates.RecommendationDataTemplate">
    
    
    <Frame Padding="0" Grid.ColumnSpan="3">
        <Grid RowDefinitions="*, Auto, Auto, Auto" ColumnDefinitions="10, *, 10">
            <Image
                Grid.RowSpan="4"
                Aspect="AspectFill"
                Grid.ColumnSpan="3"
                Source="{Binding Image}"/>
            <Label
                Grid.Row="1"
                Grid.Column="1"
                Text="Recommendations"
                TextColor="{StaticResource MidGray}"/>
            <Label
                Grid.Row="2"
                Grid.Column="1"
                FontSize="Title"
                TextColor="{StaticResource White}"
                Text="{Binding Name}"/>
            <Label
                Grid.Row="3"
                Grid.Column="1"
                FontSize="Title"
                FontAttributes="Bold"
                TextColor="{StaticResource Black}"
                Text="{Binding Price, StringFormat='${0}'}"/>
        </Grid>
    </Frame>
</DataTemplate>
